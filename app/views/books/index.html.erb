
<ul class="list-group list-group-numbered">
  <% @books.each do |book| %>
    <li class="list-group-item">
      <%= link_to book.title, book %>
    </li>
  <% end %>
</ul>
<br>
<%= button_to "New Book", new_book_path, method: :get, class:"btn btn-outline-primary"%>